/**
 * Shared Tailwind class tokens for the SEO-page section components, so every
 * section keeps one consistent rhythm and type scale.
 */

export const inner = 'max-w-400 mx-auto px-6 lg:px-[clamp(0.25rem,4vw,5rem)]';
export const sectionPad = 'py-16 lg:py-24';
export const eyebrow = 'font-display text-xs font-extrabold uppercase tracking-[0.14em] text-brand';
export const heading =
  'mt-3 font-display text-xl sm:text-2xl lg:text-[2rem] font-normal tracking-tight text-primary-comfy-canvas';
export const body = 'font-body text-content-secondary opacity-80';
export const leadProse = 'text-[1.0625rem] leading-[1.75]';
