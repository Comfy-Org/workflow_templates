/**
 * Shared chrome for the small controls that sit on a code block, so the copy
 * and download affordances stay identical wherever they are mounted — inline in
 * a block header, or overlaid on a block that has none.
 */
export const codeBlockControl =
  'cursor-pointer rounded-lg border border-white/15 bg-hub-surface p-1.5 text-hub-muted transition-colors hover:bg-hub-surface-hover hover:text-content';
