# Demo fixtures are local-only. The keyframes and rendered videos that make this
# page runnable are whatever you point it at locally, and are frequently photos
# or renders that have not been cleared for publication — so nothing in this
# directory is committed. Drop cleared assets in and remove this file (or add
# explicit negations) when the demo ships.
*
!.gitignore
!README.md
