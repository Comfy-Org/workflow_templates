# The demo's own example assets are committed (see README.md). Anything else
# dropped in here while testing — other renders, personal photos, scratch
# inputs — is not, so it can't be published by accident.
*
!.gitignore
!README.md
!example.mp4
!keyframes/
!keyframes/kf_1.webp
!keyframes/kf_2.webp
!keyframes/kf_3.webp
