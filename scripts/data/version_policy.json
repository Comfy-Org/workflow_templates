{
  "$schema": "version policy for CI auto-bump",
  "description": "Packages listed in frozen_packages are never auto-bumped by ci_version_manager.py. Bump them manually in packages/*/pyproject.toml when intentionally releasing legacy media wheels.",
  "frozen_packages": [
    "media_api",
    "media_video",
    "media_image",
    "media_other"
  ]
}
