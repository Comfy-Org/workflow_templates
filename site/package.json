{
  "name": "comfyui-template-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "astro dev",
    "sync": "tsx scripts/sync-templates.ts",
    "sync:tutorials": "tsx scripts/sync-tutorials.ts",
    "generate:ai": "tsx scripts/generate-ai.ts",
    "generate:ai:test": "tsx scripts/generate-ai.ts --test",
    "generate:ai:template": "tsx scripts/generate-ai.ts --template",
    "generate:previews": "tsx scripts/generate-previews.ts",
    "prebuild": "pnpm run sync && pnpm run sync:tutorials && pnpm run generate:ai && pnpm run generate:previews",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.1",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.11",
    "canvas": "^2.11.0",
    "openai": "^4.0.0",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.8.3"
  }
}
