---
import Analytics from '@vercel/analytics/astro';
import SpeedInsights from '@vercel/speed-insights/astro';
import PostHogAnalytics from '../components/PostHogAnalytics.astro';
import SEOHead from '../components/SEOHead.astro';
import '../styles/global.css';
import { LANGUAGES, type Locale } from '../i18n/config';
import { getLocaleFromPath } from '../i18n/utils';

const GA_ID = import.meta.env.PUBLIC_GA_MEASUREMENT_ID;

interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  ogImage?: string;
  ogDescription?: string;
  ogType?: 'website' | 'article';
  structuredData?: object;
  locale?: Locale;
  hreflangBasePath?: string;
  /** Whether localized variants of this route exist (controls hreflang alternates). */
  hreflangLocalized?: boolean;
  /** Explicit per-page alternate locales; overrides hreflangLocalized. */
  hreflangLocales?: readonly Locale[];
  theme?: 'light' | 'dark';
  hideDefaultFooter?: boolean;
  noindex?: boolean;
  /** Emit `max-video-preview:0` — see SEOHead. */
  suppressVideoIndexing?: boolean;
}

const {
  title,
  description,
  canonicalUrl,
  ogImage,
  ogDescription,
  ogType,
  structuredData,
  locale: propLocale,
  hreflangBasePath,
  hreflangLocalized = true,
  hreflangLocales,
  theme = 'light',
  hideDefaultFooter = false,
  noindex = false,
  suppressVideoIndexing = false,
} = Astro.props;

const hasCustomFooter = Astro.slots.has('footer');

// Detect locale from URL if not provided
const locale = propLocale || getLocaleFromPath(Astro.url.pathname);
const langInfo = LANGUAGES[locale];
const htmlDir = langInfo?.dir || 'ltr';

// Theme-aware classes
const isDark = theme === 'dark';
const bodyClasses = isDark
  ? 'min-h-screen flex flex-col bg-page text-content antialiased'
  : 'min-h-screen flex flex-col bg-white text-gray-900 antialiased';
---

<!doctype html>
<html lang={locale} dir={htmlDir} class="scroll-smooth">
  <head>
    {GA_ID && (
      <>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}></script>
        <script is:inline define:vars={{ GA_ID }}>
          window.dataLayer = window.dataLayer || [];
          function gtag() {
            // gtag.js requires the raw Arguments object here, not an array, or the
            // js/config commands are misinterpreted. Keep Google's official shim.
            // eslint-disable-next-line prefer-rest-params
            dataLayer.push(arguments);
          }
          window.gtag = gtag;
          gtag('js', new Date());
          gtag('config', GA_ID);
        </script>
      </>
    )}
    <SEOHead
      title={title}
      description={description}
      canonicalUrl={canonicalUrl}
      ogImage={ogImage}
      ogDescription={ogDescription}
      ogType={ogType}
      structuredData={structuredData}
      hreflangBasePath={hreflangBasePath}
      hreflangLocalized={hreflangLocalized}
      hreflangLocales={hreflangLocales}
      noindex={noindex}
      suppressVideoIndexing={suppressVideoIndexing}
    />
    <slot name="head" />
  </head>
  <body class={bodyClasses}>
    <main class="flex-1">
      <slot />
    </main>

    {
      hasCustomFooter || hideDefaultFooter ? (
        <slot name="footer" />
      ) : (
        <footer>{/* footer here */}</footer>
      )
    }
    <Analytics />
    <SpeedInsights />
    <PostHogAnalytics />
  </body>
</html>
